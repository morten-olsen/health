{
  "name": "health",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "oura:cli": "pnpm --filter @morten-olsen/health-integration-oura cli"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.6.0"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "turbo": "^2.9.4",
    "vitest": "^4.1.2"
  }
}
